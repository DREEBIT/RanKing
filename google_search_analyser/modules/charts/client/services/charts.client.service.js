'use strict';

//Records service used for communicating with the records REST endpoints
/*
angular.module('charts').factory('Records', ['$resource',
  function ($resource) {
    return $resource('api/records/:recordId', {
      articleId: '@_id'
    }, {
      update: {
        method: 'PUT'
      }
    });
  }
]);
*/
